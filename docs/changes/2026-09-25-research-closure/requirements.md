# 研究闭环收敛

目标：移除活动手册中的退休 manifest/流水线参数与旧当前状态；保留原历史摘录，增加真实 CLI 对照测试。后续 ADK 效果评测仅消费完整批准证据，不因 test-only 统计通过自动采用或升级运行资格。

验收：活动手册无 manifest.yaml/旧 sync 参数；CLI help 对照通过；历史原文未修改；完整 Root CI 与签名晋级仍独立通过。

回滚：回退文档和测试提交，不改动运行资产、历史证据或来源 pin。

# Knowledge Hub Candidate: llm_agent / agent-dev-kit 全面优化与可演进治理

- source: `reports/architecture/llm-agent-adk-target-architecture-2026-07-30.md`
- captured_at: 2026-07-30
- last_verified: 2026-07-30
- topic: llm-agent-adk-comprehensive-optimization
- candidate_kind: decision
- intended_domain: projects/llm-agent
- status: reviewing
- owner_review_required: true
- review_after: 2026-08-30

## Summary

`llm_agent` 应继续作为 reference intake、采纳决策、证据和成熟度治理工作区；`agent-dev-kit` 应继续作为平台中立的 Agent 资产编译、安装、评测和发布控制面。两者都不应扩张为 LLM runtime。

2026-07-30 复核确认：软件控制面已有较强 source/test/runtime-local 能力，但总体仍是 `M3 / release-candidate`。本轮已补齐 maintainability growth budget、Python 3.11/3.12 受支持 parity 和过期官方来源复核；真实多 runtime、独立仓库、第二操作者和 30 天现场周期仍不能通过增加 fixture 或修改状态替代。

## Decisions

1. `manifests/report_registry.json` 是 current 架构报告的唯一选择入口；checker、test 和 README 不再硬编码 dated current 路径。
2. `manifests/comprehensive_optimization_backlog.json` 保留 G1-G10 跨项目基线，并允许连续的 G11+ 项目扩展；当前 source report 必须与 registry current 一致。
3. 历史架构报告只作 provenance；当前 backlog 扩展不得要求重写历史报告。
4. blocked backlog 项必须写清 blocking condition；设计完成、实现完成、runtime 应用和 Knowledge promotion 继续分离。
5. 7 项 maintainability budget 已覆盖 scripts/reports/manifests 数量和 root/ADK 最大 shell/Python hotspot；warning 进入 review，hard limit fail，strict 模式阻断 warning。
6. ADK release evidence 只接受固定 Python 3.11/3.12 parity 与精确依赖环境；两版本 full suite 各 57/57。Python 3.8 quick pass 只能作为 development evidence。
7. 过期官方模型目录已基于 OpenAI official source 选择 `update`，新 expiry 为 2026-10-28；模型目录保持 watch-only，不硬编码 ADK 默认模型。
8. `~/.codex` 仍只允许经 `agent-dev-kit -> ~/codex -> ~/.codex` source-to-live 链路更新；本轮未执行。

## Verification

- `rtk scripts/check-architecture-reports.sh . --summary-json`：pass；3 份受治理报告、16 个连续 backlog 项、current/source 一致。
- `rtk tests/run_all.sh --timing-json /tmp/llm-agent-optimization-search-root-full-final-2026-07-30.json`：18/18 pass。
- `rtk bash agent-dev-kit/tests/test_templates.sh`：19/19 pass。
- `rtk bash agent-dev-kit/scripts/devkit.sh target check --all --level static --summary-json`：3 个 experimental target 静态合同 pass。
- `rtk bash agent-dev-kit/scripts/devkit.sh benchmark run --iterations 3 --output /tmp/adk-comprehensive-assessment-benchmark.json --summary-json`：本地预算 pass。
- `rtk bash agent-dev-kit/scripts/run-local-ci-parity.sh --python all --mode full`：Python 3.11.15 与 3.12.13 各 57/57，dependency audit 无已知漏洞。
- `rtk scripts/check-maintainability-budgets.sh --strict --summary-json`：7/7 pass；root reports 相对 baseline 漂移 +12，仍低于 warning=320。
- `rtk bash agent-dev-kit/scripts/devkit.sh official-docs-governance --summary-json`：69 sources，failures=0。
- `rtk bash agent-dev-kit/scripts/devkit.sh doctor --summary-json`：宿主 fail；Python 3.8.10 与旧依赖明确 development-only，release evidence 由 parity 提供。
- `rtk scripts/check-current-status-consistency.sh . --summary-json`：fail；last-verified baseline 过期且 ADK commit/gitlink/lock/worktree 未对账。

## Sanitization

- 未收录凭证、环境变量值、私有端点、raw session、完整日志、cache、二进制或临时 runtime state。
- 临时 timing/benchmark 文件只作为命令证据路径，不复制正文。
- 用户已有 dirty 只记录分类和阻塞影响，不复制改动内容。

## Provenance

- Source project：`llm_agent`。
- Primary source artifact：`reports/architecture/llm-agent-adk-target-architecture-2026-07-30.md`。
- Machine SSOT：`manifests/comprehensive_optimization_backlog.json`、`manifests/report_registry.json`、`manifests/product_maturity_scorecard.json`。
- Generated by AI：yes；role=`drafted`；tool=`Codex`。

## Residual Risk

- 本文件只是仓内 `reviewing` candidate，不是 Knowledge Hub active 条目。
- `agent-dev-kit` 和根仓存在用户原有 dirty；本轮未提交、清理或同步 gitlink/lock。
- runtime campaign、独立仓、第二操作者、30 天和 field events 仍是外部证据缺口。

## Memory Candidate

no。该结论属于项目架构与治理决策，不静默提升为全局 memory 或 AGENTS 规则。

## Gate Result

`reviewing / source-staged`。早期 `knowledge-capture.sh --dry-run --json` 生成只读 transaction `kh-20260730T134750Z-e0db299f`；该 transaction 未 apply，不能代表当前候选已进入 Hub。只有 owner review 后才考虑重新 capture/apply，禁止自动 active promotion。

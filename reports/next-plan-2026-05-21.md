# llm_agent 后续推进计划（2026-05-21）

## 1. 当前快照

- 工作区定位：`llm_agent` 负责吸收外部参考子仓，`agent-dev-kit` 是落地目标仓（非来源仓）。
- adoption-matrix：`total=41, adopt=38, reject=3, done=41, pending=0, blocked=0`。
- 最近 30 天命中变更子仓（含目标仓）：`agent-skills`、`codex-cookbook`、`agent-dev-kit`、`hermes-agent`、`hermes-collaboration-skill`、`mattpocock-skills`、`skills`、`superpowers`、`Trellis`、`agent-browser`。
- 今日已执行：`run-post-freeze-cycle`（14 天窗口），刷新 `reports/weekly-change-report.md` 与 `reports/adoption-matrix-summary.md`。

## 2. 推进目标

1. 外部来源吸收：仅对非 `adk-core` 子仓推进 re-intake。
2. 目标仓压实：`agent-dev-kit` 只走 harden/verify/handoff 闭环，不走来源吸收评估。
3. 证据门禁：每个推进动作必须有可复现命令与产物路径。

## 3. 执行波次

### Wave A（T+2 天）：来源仓再评估分拣

范围：`hermes-agent`、`mattpocock-skills`、`Trellis`、`agent-browser`、`codex-cookbook`、`agent-skills`、`skills`、`superpowers`、`hermes-collaboration-skill`。

动作：

1. 逐仓生成 30 天变更证据片段（SKILL/README/scripts/workflow）。
2. 形成“可吸收 / 继续观察 / 保持拒绝”三类候选清单。
3. 对 `Trellis`、`agent-browser` 保持 `reject` 的前提下，单列 optional 试点条目（不进入 core）。

验收：

- `reports/weekly-change-report.md` 更新且包含上述来源仓条目。
- 至少形成 1 份汇总候选清单（见本文件第 4 节 CSV）。

### Wave B（T+7 天）：落地与验证

动作：

1. 选 2~3 个高收益候选落地到 `agent-dev-kit`（优先 skill 治理、证据闭环、routing 边界）。
2. 每个落地项补充对应 evidence 路径（docs/runbooks 或 skills）。
3. 运行门禁回归：
   - `rtk bash scripts/check-all.sh --quick`
   - `rtk bash scripts/check-upstream-intake-readiness.sh`
   - `rtk bash scripts/check-adoption-evidence-integrity.sh`

验收：

- 三条命令全 PASS。
- adoption-matrix 不出现 `pending`/`blocked`。

### Wave C（T+14 天）：目标仓压实与交接

动作：

1. `agent-dev-kit` 侧完成一次压实闭环（harden + full regression）。
2. 验证 `~/codex -> ~/.codex` live footprint 与 pilot readiness。
3. 更新 `phase-gate.env` 复审证据（仅在门禁结果变化时调整字段）。

验收：

- `rtk bash scripts/check-all.sh --verbose` PASS。
- `reports/codex-pilot-report.md` 保持可复现证据链。

## 4. 任务看板（机器可读）

配套文件：`reports/next-plan-2026-05-21.csv`

## 5. 风险与边界

1. 不把 `agent-dev-kit` 作为来源仓加入 adoption re-intake。
2. 不新增 `pending` 状态作为“待办占位”，待办统一放在本计划与 CSV。
3. 对 `reject` 项只允许 optional 试点，不直接改核心路由。

## 6. 复盘触发器

满足任一条件立即重跑本计划：

1. `scripts/check-all.sh --quick` 出现 FAIL。
2. 任一 P0/P1 子仓在 14 天内出现新的 SKILL/AGENTS/workflow 重大变更。
3. `phase-gate.env` 进入复审窗口（`next_review_by` 临近）。

# 阶段门禁（SSOT）
# 目标：先压实 global-dev-kit，再追踪参考子仓增量。

phase=harden-gdk
allow_upstream_sync=yes

# 打开条件（全部满足再改为 yes）：
# 1) gdk 严格校验通过
# 2) gdk optional skills 回归通过
# 3) gdk 外部引用门禁通过
# 4) codex 试跑至少 1 个高风险场景并沉淀报告
# 5) 全局 ~/.codex doctor 健康检查通过（errors=0）

opened_on=2026-05-02
owner=
note=default-block-upstream-tracking
